#include <iostream>
#include <cmath>
#include <stdio.h>
using namespace std;

int main() {
  int cont, inst, qtd;
 while ((cin >> qtd) != NULL) {
  if ((qtd >=1) && (qtd <= 100)) {
    int valores[qtd];
   for (int i = 0; i < qtd; i++) {
     cin >> valores[i];
   }

  }
 }
  return 0;
}
